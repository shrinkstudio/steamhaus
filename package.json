{
  "name": "steamhaus",
  "version": "1.0.0",
  "description": "Steamhaus Webflow site — JS bundle served via JSDelivr",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
